[build]
  command = "npm run build:ssr"
  publish = "dist/gerador-qrcode/browser"
  functions = "dist/gerador-qrcode/functions"

[functions]
  node_bundler = "esbuild"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/ssr"
  status = 200

[build.environment]
  NODE_VERSION = "18"
